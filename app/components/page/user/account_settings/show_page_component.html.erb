<div class="flex justify-center">
  <%= render Ui::PanelComponent.new(size: :medium) do |panel| %>
    <%- panel.with_panel_content do %>
      <h1>アカウント設定</h1>

      <div class="mt-4">
        <%= render Domain::User::AccountSettingsComponent.new(user: @user) %>
      </div>
    <% end %>
  <% end %>
</div>
